spring.application.name=study

# URL de conexão com o banco 'shop'
spring.datasource.url=jdbc:mysql://localhost:3306/shop?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Credenciais de acesso
spring.datasource.username=root
spring.datasource.password=Ca25615947

# Driver de conexão
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações do Hibernate / JPA
# validate: apenas valida a estrutura existente, protegendo suas tabelas manuais
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

